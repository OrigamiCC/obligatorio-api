'use strict';

/**
 * evento-guardado controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::evento-guardado.evento-guardado');
